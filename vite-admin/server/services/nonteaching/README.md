# Non-teaching services

- Place non-teaching service logic here to avoid collisions with teaching services.
- If you add a service, re-export it via a small shim in `services/` if existing code needs to import it from the legacy path.
