# Non-teaching routes

- Add non-teaching Express routers here so they stay isolated from teaching work.
- If you add a router, create a tiny shim in `routes/` (e.g., `routes/nonteaching-applications.js`) to keep server imports stable.
