# Non-teaching components

- Build non-teaching UI here to avoid collisions with teaching components.
- Add pages/layouts/dashboards in this folder; if you need to expose them to existing routes, add a small shim in `src/components/`.
